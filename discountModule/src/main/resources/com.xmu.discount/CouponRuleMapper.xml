<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xmu.discount.mapper.CouponRuleMapper">
    <resultMap id="couponRule" type="couponRulePo" autoMapping="true">
        <result property="beDeleted" column="is_deleted"/>
    </resultMap>


    <select id="getCouponRuleById" parameterType="Integer" resultMap="couponRule">
        select id,
               name,
               brief,
               begin_time,
               end_time,
               pic_url,
               valid_period,
               strategy,
               total,
               goods_list1,
               goods_list2,
               gmt_create,
               gmt_modified,
               is_deleted

        from coupon_rule where id = #{id}
    </select>

    <select id="listCouponRules" resultMap="couponRule">
        select id,
               name,
               brief,
               begin_time,
               end_time,
               pic_url,
               valid_period,
               strategy,
               total,
               goods_list1,
               goods_list2,
               gmt_create,
               gmt_modified,
               is_deleted

        from coupon_rule where id!=0
    </select>

    <insert id="addCouponRule" parameterType="couponRulePo" useGeneratedKeys="true" keyProperty="id">
        insert into coupon_rule(id, name, brief, begin_time, end_time, pic_url, valid_period, strategy, total, goods_list1, goods_list2, gmt_create, gmt_modified, is_deleted)
        values
        (#{id}, #{name}, #{brief}, #{beginTime}, #{endTime}, #{picUrl}, #{validPeriod}, #{strategy}, #{total}, #{goodList1}, #{goodList2}, #{gmtCreate}, #{gmtModified}, #{beDeleted})
    </insert>

    <update id="updateCouponRuleById" parameterType="couponRulePo">
        update coupon_rule
        <trim prefix="set" suffixOverrides=",">
            <if test="name!=null">name=#{name},</if>
            <if test="brief!=null">brief=#{brief},</if>
            <if test="beginTime!=null">begin_time=#{beginTime},</if>
            <if test="endTime!=null">end_time=#{endTime},</if>
            <if test="picUrl!=null">pic_Url=#{picUrl},</if>
            <if test="validPeriod!=null">valid_Period=#{validPeriod},</if>
            <if test="strategy!=null">strategy=#{strategy},</if>
            <if test="total!=null">total=#{total},</if>
            <if test="goodList1!=null">good_list1=#{goodList1},</if>
            <if test="goodList2!=null">good_list2=#{goodList2},</if>
            <if test="gmtCreate!=null">gmt_create=#{gmtCreate},</if>
            <if test="gmtModified!=null">gmt_modified=#{gmtModified},</if>
            <if test="beDeleted!=null">is_deleted=#{beDeleted},</if>
        </trim>
        WHERE id=#{id}
    </update>

</mapper>